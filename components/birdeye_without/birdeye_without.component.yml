name: Birdeye Without
description: 'Birdeye Without component using React from a CDN.'
js:
  birdeye_without.js: {attributes: { type: 'text/babel' }}
css:
  birdeye_without.css: {}
# This component makes use of an existing shared library which is provided by
#   another component (library type). This is how to share and manage common
#   assets across different components.
dependencies:
 - component/example_react_lib
cache:
  max-age: 0
